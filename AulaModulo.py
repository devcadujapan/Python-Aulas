"""import Par
print(Par.par_impar(89))

import random
print(random.random())

from random import random
print(random())

from random import random, choice
print(random())

lista = ["Pedra", "Papel", "Tesoura"]
print(choice(lista))

from random import (
    random,
    choice
)
print(random())

lista = ["Pedra", "Papel", "Tesoura"]
print(choice(lista))"""

from random import (
    random as rd
)
print(rd())

#import random
#from random import random
#import random as rd
